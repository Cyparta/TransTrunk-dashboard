export const metadata = {
    title: "Login page",
    description: "Login page description",
};

export default function LoginLayout({ children }) {
    return (
        <>
            {children}
        </>
    );
}
